---
import { Image } from "astro:assets";

interface Props {
  id: string;
  imagen: string;
  alt: string;
  date?: string;
  customHeight?: string;
}

const { imagen, date, id, alt, customHeight = "auto" } = Astro.props;
---

<div class="pb-4">
  <!-- <Image
    width="100"
    height="100"
    typeof="image/webp"
    src={imagen}
    style={{ height: customHeight }}
    class="masonry-img w-full object-cover object-center rounded-xl"
    alt={alt}
  /> -->
  <!-- Bug with astro image -->
  <img
    loading="lazy"
    src={imagen}
    alt="cosas"
    class="masonry-img w-full object-cover object-center rounded-xl"
  />
</div>
