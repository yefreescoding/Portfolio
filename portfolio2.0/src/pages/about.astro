---
// import Layouts
import MainLayout from '../Layout/MainLayout.astro';

// import components
import Cover from '../Components/Cover.astro';
import QuickLinks from '../Components/Quick-links.astro';

// import data
import aboutMe from '../Data/aboutMeData';
---

<MainLayout subtitle="About">
  <Cover
    coverTitle="Yefree Valdez"
    description="Aqui podras conocerme un poco mas sobre mi , mi experiencia, habilidades y algunas de mis aficiones."
    coverImage="/images/hero-images/about-hero.PNG"
  />
  <section id="about" class="section-about">
    <article class="section-about__article">
      {aboutMe.map((p) => <p>{p.para}</p>)}
    </article>
    <div class="section-about__div">
      <article class="section-about__article">
        <h2 class="section-about__h3">Tecnologias</h2>
        <p>
          Como desarrollador de paginas y aplicaiones web, puedo decir que
          domino los lenguajes fundamentales HTML, CSS y JavaScript. Tambien
          tengo entendimiento basico de PHP y MySQL.
        </p>
        <p>
          Tambien domino las herramientas para que mi trabajo sea mas rapido,
          efectivo y eficaz. Herramientas como git, npm, Gulp, NodeJS, ViteJS y
          nvx.
        </p>
        <p>
          Actualmente me encuentro desarrollando las mayoria de mis proyectos
          utilizando la libreria de ReactJS y con la ayuda de frameworks como
          NextJS y Astro.
        </p>
      </article>
      <article class="section-about__article">
        <h2 class="section-about__h3">Objetivos</h2>
        <p>
          Mis metas las tengo bastante claras. Poder dedicarme de manera
          profesional a la creacion de software para agragar valor a la vida de
          las personas. Poder formar parte deun equipo, un grupo o un movimiento
          creo que es algo que me llenaria bastante.
        </p>
      </article>
    </div>
  </section>
</MainLayout>

<style lang="scss">
  .section-about,
  .section-about__article,
  .section-about__div {
    display: grid;
    gap: 1rem;
  }

  .section-about {
    grid-template-columns: repeat(auto-fit, minmax(min(20rem, 100%), 1fr));
    color: var(--clr-text-1);
  }

  .section-about__article {
    padding: 2rem;
    outline: 2px solid var(--clr-border-2);
    background-color: var(--clr-background-2);
    position: relative;
    --distance: 6px;

    &::after,
    &::before {
      content: '';
      position: absolute;
      inset: 0;
      border: inherit;
      z-index: -1;
    }
    &::after {
      transform: translate(var(--distance), var(--distance));
      background-color: var(--clr-background-1);
    }
    &::before {
      transform: translate(
        calc(var(--distance) * 2),
        calc(var(--distance) * 2)
      );
      background-color: var(--clr-text-2);
    }
  }
</style>
